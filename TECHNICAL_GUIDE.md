## 📚 دليل التطوير والتقني

### البنية العامة للمشروع

```
QuizApp/
├── lib/
│   ├── main.dart                    # نقطة الدخول للتطبيق
│   ├── app/
│   │   └── quiz_app.dart           # تكوين التطبيق الرئيسي
│   └── core/
│       ├── features/               # الميزات الرئيسية
│       ├── models/                 # نماذج البيانات
│       ├── data/                   # البيانات والمعلومات
│       ├── services/               # الخدمات
│       ├── router/                 # نظام التوجيه
│       ├── styles/                 # الألوان والنمط
│       └── widgets/                # الـ Widgets المشتركة
├── assets/
│   └── images/                    # الصور والأيقونات
├── test/
│   └── widget_test.dart           # اختبارات الـ Widgets
└── pubspec.yaml                   # معلومات المشروع والمتعلقات

```

---

## 🔄 تدفق التطبيق

```
┌─────────────┐
│  Splash     │  -> شاشة البداية مع الرسوم المتحركة (3 ثوان)
│   Screen    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│    Home     │  -> الصفحة الرئيسية مع قائمة الاختبارات
│   Screen    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│  Assessments List       │  -> قائمة الاختبارات المتاحة
│  (DASS-21, AUTISM)      │
└──────┬──────────────────┘
       │
       ▼
┌──────────────────┐
│  Assessment      │  -> نموذج الاختبار التفاعلي
│  Screen          │
│  (21 أسئلة)      │
└──────┬───────────┘
       │
       ▼
┌──────────────────────┐
│  Results Analysis    │  -> عرض النتائج والتحليل
│  & Recommendations   │
└─────────────────────┘
```

---

## 📊 نماذج البيانات

### 1. Assessment Model
```dart
class Assessment {
  final String id;
  final String title;
  final String description;
  final String assessmentType; // DASS, AUTISM, etc.
  final List<Question> questions;
  final String language;
}
```

### 2. Question Model
```dart
class Question {
  final String id;
  final String text;
  final String category; // depression, anxiety, stress
  final List<Answer> answers;
  final int order;
}
```

### 3. AssessmentResult Model
```dart
class AssessmentResult {
  final String id;
  final String assessmentId;
  final List<UserResponse> responses;
  final int totalScore;
  final Map<String, int> categoryScores;
  final String overallSeverity;
  final DateTime completionDate;
  final String interpretation;
  final List<String> recommendations;
}
```

---

## 🎨 نظام الألوان

### AppColors
```dart
class AppColors {
  static const Color primaryColor = Color(0xFF1e3c72);    // أزرق داكن
  static const Color secondaryColor = Color(0xFF2a5298);  // أزرق
  static const Color whiteColor = Color(0xFFFFFFFF);      // أبيض
  // إضافة ألوان أخرى حسب الحاجة
}
```

---

## ✏️ نظام الخطوط

### TextStyles المستخدمة:
- **Cairo Font**: للنصوص العربية (احترافية وسهلة القراءة)
- **Montserrat Font**: للنصوص الإنجليزية

### أمثلة الاستخدام:
```dart
Text(
  'مرحباً',
  style: AppTextStyles.cairo24w700,  // حجم 24، وزن 700 (عريض)
)
```

---

## 🔌 خدمات التحليل

### AssessmentAnalysisService

#### معالجة DASS-21:
1. حساب النقاط لكل فئة (depression, anxiety, stress)
2. تحديد مستوى الخطورة بناءً على النقاط
3. توليد تفسير مفصل
4. إنشاء توصيات مخصصة

#### معالجة Autism:
1. حساب النقاط الإجمالية والفئات
2. تحديد مستوى الأعراض
3. تحليل كل فئة من الفئات الثلاث
4. توصيات علاجية

---

## 🎯 نقاط القرار في التحليل

### DASS-21:
```
النطاق (0-63) لكل فئة:
- 0-4: طبيعي
- 5-9: خفيف
- 10-14: معتدل
- 15-19: شديد
- 20+: شديد جداً
```

### Autism:
```
مقياس (0-30):
- 0-9: منخفض
- 10-19: معتدل
- 20-24: عالي
- 25+: عالي جداً
```

---

## 🔐 نقاط أمان البيانات

1. **عدم تخزين البيانات الشخصية**: لا نطلب أي معلومات شخصية
2. **بيانات محلية فقط**: البيانات تُحفظ محلياً فقط (في التطوير المستقبلي)
3. **عدم الاتصال بالإنترنت**: التطبيق يعمل بدون الاتصال
4. **تنويهات قانونية**: تحذيرات واضحة في كل اختبار

---

## 🚀 التطوير والتوسع المستقبلي

### ميزات مخطط إضافتها:
1. **قاعدة بيانات محلية** (Hive/SQLite)
   - حفظ تاريخ الاختبارات
   - تتبع التطور
   - إنشاء رسوم بيانية للتطور

2. **اختبارات إضافية**:
   - مقياس ADHD
   - مقياس القلق العام (GAD-7)
   - مقياس الاكتئاب (PHQ-9)
   - مقياس الوسواس القهري (YBOCS)

3. **تقارير PDF**:
   - تحميل النتائج كملف PDF
   - رسوم بيانية تفصيلية

4. **نظام المستخدمين**:
   - تسجيل الدخول (اختياري)
   - حفظ البيانات السحابية
   - مقارنة النتائج عبر الوقت

5. **التكامل مع المتخصصين**:
   - الاتصال بمعالجين نفسيين
   - المشورة المباشرة

6. **الترجمة**:
   - دعم لغات إضافية
   - محتوى محلي متخصص

---

## 🧪 الاختبار والجودة

### اختبارات الوحدة:
```dart
test('DASS-21 scoring calculation', () {
  // اختبار حساب النقاط
});

test('Autism severity determination', () {
  // اختبار تحديد الشدة
});
```

### اختبارات الواجهة:
```dart
testWidgets('Assessment screen navigation', (tester) async {
  // اختبار التنقل والواجهة
});
```

---

## 📱 متطلبات الجهاز

### الذاكرة:
- الحد الأدنى: 2 GB RAM
- الموصى به: 4 GB RAM

### مساحة التخزين:
- حجم التطبيق: ~50 MB

---

## 🔧 أوامر مفيدة

```bash
# تنظيف المشروع
flutter clean

# الحصول على المتعلقات
flutter pub get

# تشغيل الاختبارات
flutter test

# بناء الإصدار
flutter build apk --release

# تحليل الأداء
flutter run --profile

# عرض معلومات الجهاز
flutter devices
```

---

## 📝 ملاحظات التطوير

### أفضل الممارسات:
1. استخدم GetX للحالة والتوجيه
2. اتبع Clean Architecture
3. استخدم الثوابت لتجنب تكرار القيم
4. أضف تعليقات للكود المعقد
5. اختبر على أجهزة حقيقية

### معايير الكود:
- اسم المتغيرات واضح ومختصر
- استخدم camelCase للمتغيرات
- استخدم PascalCase للـ Classes
- إضافة documentation comments

---

**آخر تحديث**: فبراير 2026
